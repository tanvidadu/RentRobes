<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.tanvidadu.learnit">

    <uses-feature
        android:name="android.hardware.camera"
        android:required="true" />

    <uses-permission android:name="android.permission.RECEIVE_SMS" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <permission android:name="android.permission.INTERNET" />
    <permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <uses-feature
        android:name="android.software.live_wallpaper"
        android:required="true" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <meta-data
            android:name="com.google.android.gms.version"
            android:value="@integer/google_play_services_version" />

        <activity android:name=".MainActivity" />
        <activity android:name=".SellOption">

        </activity>
        <activity android:name=".GenerateRequest" />
        <activity android:name=".generate_request_address" />
        <activity android:name=".RequestSummary" >

        </activity>
        <activity android:name=".LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".TypeWiseList" />
        <activity android:name=".Catalog" />
        <activity android:name=".Settings" />
        <activity android:name=".ListItem" />

        <meta-data
            android:name="com.razorpay.ApiKey"
            android:value="rzp_test_ivmMqQHZoRea5L" />

        <activity android:name=".MerchantActivity" />
        <activity android:name=".PickDate" />
        <activity android:name=".Temporary" />
        <activity android:name=".SignUp" />
    </application>

</manifest>